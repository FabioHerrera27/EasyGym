# EasyGym

Repositorio del proyecto EasyGym, elaborado por Diego Villa, Santiago Garcia y Fabio Herrera.

## Desarrollo

1. Instala las dependencias con `npm install`
2. Inicia el entorno local con `npm run dev`
3. Genera la build con `npm run build`
